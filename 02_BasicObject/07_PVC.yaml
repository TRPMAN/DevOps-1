---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  # this name will use for mapping
  name: mysql-pvc
  labels:
    app: wordpress
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: default
  resources:
    requests:
      storage: 1Gi